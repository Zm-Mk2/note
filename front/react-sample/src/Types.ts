export type Note = {
    id: string
    content: string
}

export type Coord = {
    left: number
    top: number
    right: number
    bottom: number
}

export type Window = {
    width: number
    height: number
}